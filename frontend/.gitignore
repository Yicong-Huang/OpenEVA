# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# This project is managed with yarn. An `npm install` here rewrites
# yarn.lock to npmjs URLs and drops the cross-platform optional binaries
# (rolldown/rollup bindings), which breaks installs on other platforms.
package-lock.json
