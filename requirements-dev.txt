# Eva test + lint dependencies. Install with:
#   .venv/bin/pip install -r requirements-dev.txt
# (Includes everything in requirements.txt -- pip transitively resolves.)

-r requirements.txt

# Test runner + coverage
pytest>=8,<10
pytest-cov>=5,<8

# FastAPI TestClient sits on top of httpx
httpx>=0.27,<1

# Static-analysis tools used by tests/test_doc_accuracy.py and the
# CLAUDE.md "loop playbook" steps.
pyflakes>=3,<4
